super + control + {t, q, c, b, s, d, s}
	{kitty, qalc, code, microsoft-edge-stable, spotify, discord, flameshot gui}

super + @space
    rofi -show drun

super + shift + e
    rofi -show filebrowser

super + canc
    sh powermenu.sh

alt + f4
    sh powermenu.sh

super + w
	rofi -show window

super + l
    xdg-screensaver lock
