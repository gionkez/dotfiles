general {
    ignore_empty_inputs = true
    hide_cursor = true
}

animation {
    enabled = false
}

background {
    blur_size = 3
    blur_passes = 3
    path = screenshot
}

input-field {
    inner_color = rgba(FFFCFFFF)
    outline_thickness = 3
    placeholder_text = 
    fail_color = rgba(EEB00BFF)
    fail_text = wrong password
    rounding = 5
    outer_color = rgba(FF6D6DFF)
    font_family = Iosevka NFM
    dots_center = true
    valign = center
    font_color = rgba(171917FF)
    halign = center
    dots_size = 0.1
    size = 100, 50
    dots_spacing = 0.3
}

label {
    position = 0, 50
    color = rgba(FFFCFFFF)
    font_family = Iosevka NFM
    text = $TIME
    font_size = 24
}